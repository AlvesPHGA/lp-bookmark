import { tv } from 'tailwind-variants';

export const buttonStyle = tv({
   base: 'block px-6 py-1 uppercase rounded-md tracking-wider border border-red-500 bg-red-500 text-white font-medium hover:text-red-500 hover:bg-transparent transition',
});
